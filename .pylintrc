[TYPECHECK]
generated-members=torch.*

[MESSAGES CONTROL]
# 屏蔽指定的警告（添加到已有的 disable 列表中，用逗号分隔）
disable=
    W0603,   # Using the global statement
    W0143,   # Comparing against a callable
    R0903,   # Too few public methods
    E0401,   # Import "torch" should be placed at the top of the module